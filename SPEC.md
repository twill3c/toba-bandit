# SPEC.md — toba-bandit

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

当たり確率が隠された 5 台のスロット台(ベルヌーイ多腕バンディット)を、
ε-greedy・UCB1・Thompson サンプリングの 3 戦略が同時に打ち続け、
累積後悔(regret)のレースで「探索と活用のトレードオフ」を見せる教材アプリ。
真の確率が既知なので後悔は厳密に計算でき、「UCB/Thompson の平均後悔は 0 へ向かい、
固定 ε の ε-greedy は下げ止まる」という理論の要点を予算付きゲートで検証する。
ブラウザ完結・静的エクスポート。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | シード付き PRNG(mulberry32)を注入。台の確率・報酬・戦略の乱択は全て決定論(戦略ごとに独立 rng 状態) | must |
| F-02 | 台: k=5 のベルヌーイ腕。当たり確率はシードから生成([0.15, 0.85]・相互に ≥0.05 離す・最良腕は一意)。「台の配置替え」で再抽選 | must |
| F-03 | 戦略 3 種(共通インターフェース `stepStrat`・純関数): ε-greedy(未試行優先 → ε で乱択/それ以外は平均最大・タイ最小 index)/ UCB1(全腕 1 回ずつ → 平均 + √(2ln t / nᵢ))/ Thompson(Beta(成功+1, 失敗+1) サンプリング・Marsaglia-Tsang ガンマ法) | must |
| F-04 | 後悔は擬似後悔 Σ(p\* − p_選択腕) で累積(理論曲線が滑らかに出る)。報酬合計・腕別試行数も記録 | must |
| F-05 | 可視化: 3 戦略の累積後悔レース曲線(線形軸・色+直ラベル)+ 戦略×台の試行配分バー + 直近に引いた台のハイライト | must |
| F-06 | 台パネル: 各台の推定平均(戦略別)と試行数。「真の当たり確率を表示」トグルで正解の目盛を重ねる | must |
| F-07 | 実行制御: 開始 / 一時停止 / 1 プレイ / リセット / 台の配置替え・速度 1x/10x/100x(フレーム内バッチ上限) | must |
| F-08 | ε スライダー(0〜0.5)。ε-greedy にのみ次プレイから反映 | must |
| F-09 | フッタ: MIT License・GitHub・操作説明(張り方)・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。サーバ API・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ・決定論。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 描画(rAF)とプレイを分離。1 フレームのバッチに上限 | batchSize テスト |
| N-04 | ポインタ・タッチ両対応 | 手動確認 |
| N-05 | クライアント専用 API は effect 内のみ・縁(全腕未試行等)は正常系として仕様化(HC-002 予防適用) | core テスト |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

数理ゲート(閾値・予算は較正実験の証拠つきで確定):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | ベルヌーイ報酬の整合 | シード固定 2,000 回で標本平均が真の p と ±0.05 以内・決定論 |
| G-02 | 選択則の手計算一致 | ε=0 の貪欲・UCB1 の指数式・未試行優先を極小状態で照合 |
| G-03 | 学習する戦略の平均後悔減衰 | シード 1・T=5,000: UCB と Thompson の平均後悔が T=500 時点より減少し、最終 1,000 プレイの最良腕率 ≥ 80% |
| G-04 | ε-greedy の下げ止まり | 同条件で ε=0.2 の平均後悔が UCB の 2 倍超に残る(固定探索コストの可視化) |
| G-05 | 決定論 | 同一シードの 2 回の実行が全履歴で深い等値 |

## 5. スコープ外

- 非定常バンディット・文脈付きバンディット・腕数の変更 UI
- UCB の変種(UCB-V 等)・割引・バッチ更新
- 学習結果の永続化・効果音・BGM
